node_modules
dist
.DS_Store
.env
.env.local
.env.*.local

# Ignore the output video from Git but not videos you import into src/.
out

# Remotion's bundle/render cache — appears on first render.
.remotion

# TypeScript incremental build info.
*.tsbuildinfo

# ESLint cache (only created when --cache is passed; defensive entry).
.eslintcache
