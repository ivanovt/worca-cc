<!-- append -->
## Override: Constraint

Write test files ONLY. Do NOT modify any production source files. If a test requires a change to production code to pass, document it as a finding and skip that test — do not implement the fix.
