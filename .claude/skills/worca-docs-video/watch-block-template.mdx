{/*
  Insert this <VideoPlayer> block into
    docs-site/src/content/docs/introduction/watch.mdx
  at the right position (chapters appear in order).

  Replace:
    {number}    chapter number, zero-padded: "01", "02", "03", "04", …
    {slug}      chapter slug, exactly as set in script.ts
    {title}     chapter.title from script.ts
    {subtitle}  "Video N of M · <one-line teaser>"
    {duration}  "M:SS" — read from the rendered MP4. The publish script
                logs the size; grab the duration from `ffprobe out/*.mp4`
                or by reading scene durations from audio-manifest.

  Also update the page intro line if the chapter count or total
  duration changed (e.g. "Three videos, about eleven minutes total."
  → "Four videos, about fifteen minutes total.").
*/}

<VideoPlayer
  src="/videos/{number}-{slug}.mp4"
  poster="/videos/{number}-{slug}-poster.jpg"
  captions="/videos/{number}-{slug}.vtt"
  title="{title}"
  subtitle="Video N of M · {subtitle}"
  duration="{duration}"
/>
