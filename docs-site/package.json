{
  "name": "worca-docs",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "git rev-parse --is-shallow-repository 2>/dev/null | grep -q true && git fetch --unshallow --quiet 2>/dev/null || true",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.3.1",
    "sharp": "^0.34.5",
    "starlight-image-zoom": "^0.14.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.1",
    "wrangler": "^4.95.0"
  }
}
